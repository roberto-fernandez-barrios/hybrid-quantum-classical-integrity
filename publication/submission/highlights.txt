OPTIONAL INTERNAL SUMMARY — IEEE TDSC does not currently require highlights.

Auditability is conditioned on the evidence exposed at the workflow boundary and on the reference profile; the executed Class-A sensors use statistical same-item-set aggregate comparisons against a benchmark-protected oracle, not deployed authentication.
The constructive result is claim-relative minimum evidence granularity: a trusted aggregate of the same batch certifies the reported conclusion, while item alignment is required for identity.
The calibrated rules reproduce the structural blind regions across three datasets and eight fixed environments (a consistency check); the informative quantity is how often those blind regions coincide with material conclusion changes and served decisions.
A conformal family rule has finite-sample level at most alpha under exchangeability; in the executed, non-exchangeable design the observed decision false-alarm rates are 0.048–0.058, against 0.125–0.259 for the union of per-sensor rules; the earlier asymmetric rule had no guarantee.
An adaptive attacker who preserves the feature-cluster fingerprint retains 83–91 % of the conclusion changes; aggregate material served rates are 0.39/0.28/0.29 for I_X/I_XF/I_XFY, with a 0.22–0.90 matched-strength I_XFY profile.
Stronger provenance closes blind regions at a measured cost: 85 gross exact-reference blocks overlap 46 batch interruptions, so the trusted regime adds 39/1,200 near-null interruptions (3.25 percentage points), not 85.
Policy P3 (coverage-complete abstaining) fails closed on missing coverage only and guarantees no minimum detection power.
The quantum branch is a bounded ideal-statevector/finite-shot-emulation validation, not a hardware claim; semantic, estimated and observed kernels are kept apart.
